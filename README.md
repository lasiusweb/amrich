# amrich

## Project
Amrich Pharma marketing site built with Astro.

## Quick commands
- Install: `npm install`
- Dev server: `npm run dev` (localhost:4321)
- Build: `npm run build` (outputs `dist/`)
- Preview built site: `npm run preview`

For more details, see the project README and DESIGN.md in the repo.